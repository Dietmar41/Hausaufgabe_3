%% Aufgabe 1
% Aufgabe 1.1
A = rand(4,3)
a = A(1,2)
b = A(2,3)
c = A(end,end)

% zufällige Werte zwischen 0 und 0.5
A = 0.5 * A
% ein zufälliger Wert zwischen 0 und 0.5 in allen Einträgen
A(:,:) = 0 + (0.5-0).*rand(1)

% Aufgabe 1.2
B = 3*eye(4)
%Alternative
Z = diag([3 3 3 3])

% Aufgabe 1.3
C = magic(3)
d = [1:3]'

e = C\d
f = eig(C)
